import 'package:firebase_signin/firbasechat/Authenticate/Autheticate.dart';
import 'package:flutter/material.dart';

class chatPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
       // body: Chats(),
    body:Authenticate(),
      );
}
